# 引継ぎ書 — 奥行キ消失点 / VANISHING DEPTH

Claude Code など別環境で開発を引き継ぐための資料。
**まず `dimension_puzzle_spec.md`（ゲーム仕様 v10）と `solver_spec.md`（ソルバー仕様）を読むこと。**
本書はそれらを補い、「どこに何があるか」「何を踏むと壊れるか」「どう作業を進めるか」を伝える。

---

## 1. 現在の状態（2026-07 時点）

**完成しているもの**

- コアメカニクス（3D↔2Dの圧縮/再構成、均質化、数字入れ替え、押し、重力、連結グループ、壁なし世界）
- 全14章47ステージ（第1〜7章＝チュートリアル、第8〜14章＝本編）。本編は全ステージで「折らずには解けない」ことをBFSで機械的に証明済み
- タイトル/ステージ選択/オプション/記録の各画面、サウンド（Web Audio合成）、視覚演出
- 開発者用ステージエディタ（`dp.dev(1)` で有効化）
- **ソルバー**（可否判定・par・リーク検出・近道検出・詰み検出・解の再生）
- **スコア**（手数カウント、par基準の星評価、実績バッジ）
- **次の一手ヒント**（盤面上への視覚表示）
- **記録/統計**（累計プレイ統計）
- Nodeテスト基盤（ロジック層＋jsdomページ起動層）

**未着手（提案済み・合意待ち）**

- **B: 吸収警告と敗因リプレイ** — 折りでアバター/ゴールが消える予告表示。**緊張感とのバランス（常時表示 / オプション / 一度死んだ後だけ表示）が未決定で、実装時に要相談**
- **C残り: ステージ別チャレンジ条件** — 「折り2回以内」等の裏目標
- **D: 章扉の断片詩・クリア演出の格差**
- **E: タッチ/モバイル対応・エディタ一般開放・色覚対応**

**保留中の判断事項**

- ソルバーが**想定解より大幅に短い近道**を複数のステージで発見している。特に **9-2「軟化」は par=3**（想定解は9手前後）、9-1 もx折りを絡めた6手解がある。ルール上は正当なので par としては正しいが、「章の狙いを迂回する近道」として盤面を調整するかは**ユーザー判断待ち**。エディタの「設計チェック」で近道の中身を確認できる。

---

## 2. ファイル構成

```
dimension-puzzle/
├─ index.html (= dimension-puzzle-game.html)  … 本体。全コードがここに入っている（約159KB）
├─ stages.js              … 配布用ステージ束（build_stages.py が生成。手で編集しない）
├─ build_stages.py        … stages/*.json → stages.js + manifest.json
├─ dimension_puzzle_spec.md … ゲーム仕様 v10
├─ solver_spec.md         … ソルバー仕様 draft v1
├─ README.md              … 使い方・JSON形式・操作
├─ HANDOVER.md            … 本書
├─ stages/                … ステージJSON（1ファイル1ステージ）+ manifest.json + tiers.json
├─ package.json           … テスト実行用スクリプト（npm test 等）
└─ dev-tests/             … テストとツール（配布不要）
   └─ _helper.js          … テスト共通ヘルパー（HTML探索・モジュール抽出）
```

ステージIDは `stageXXYY`（XX=章番号2桁, YY=章内番号2桁）。例: `stage0902` = 第9章の2番目。

**リポジトリ化するとき**: `index.html` / `stages.js` / `stages/` が配布物。`dev-tests/` と `build_stages.py`、各 `*.md` は開発用。GitHub Pages ならルートに配布物を置けばそのまま動く。

---

## 3. 本体HTMLの構造

単一HTMLだが、内部は明確に層が分かれている。**最後の `<script>` ブロックに全ロジックが入っている**。上から順に:

| 順序 | 内容 | 備考 |
|---|---|---|
| 1 | `const Engine=(function(){...})()` | ボクセル世界の低レベル操作。`makeWorld` `setVox` `getVox` `compress` `reconstruct` `gravitySettle`(2D) `gravitySettle3D` `assignIds` `countType` `cloneGrid` `cloneWorld` |
| 2 | `const Game=(function(){...})()` | 2D側のゲームルール。`move` `swap` `cell` `counts` `gravityDropAvatars` |
| 3 | `const Game3D=(function(){...})()` | 3D側のゲームルール。`move` `gravityDropAvatars` |
| 4 | `const FOLD={...}` | 畳む向きの定義（軸・手前・画面反転フラグ） |
| 5 | `const Solver=(function(){...})()` | ソルバー。`search` `findDeadlocks` `movesFor` `applyMove` `keyOf` `formatPath` 他 |
| 6 | `const Score=(function(){...})()` | 星評価。`starsFor` `starStr` |
| 7 | `/* ===== APP ... */` 以降 | UI・描画・入力・進行・エディタ。DOM依存 |

**テストがこの構造に依存している。** ロジック層のテストは「`const Engine=` の位置から `/* ===== APP` の位置まで」を切り出して `eval` する。この2つの目印を消したり順序を変えたりすると全テストが壊れる。

主要なグローバル状態（APP層）:

- `state` … 現在の盤面。`{mode:'3d'|'2d', world, grid2D, dims, foldFront, status, moves, hintsUsed, undoUsed, hintFx, ...}`
- `stageRules` … 現ステージのルール `{foldAllow:{...}, swapAllow}`
- `PROG` / `STATS` … localStorage 永続化（`dp_progress` / `dp_stats`）
- `ed` / `edSolverState` … エディタ状態

---

## 4. 踏むと壊れる落とし穴（実際に踏んだもの）

### 4.1 【最重要】DOMヘルパー `e` のTDZ

```js
const e = id => document.getElementById(id);   // ← スクリプト後半（エディタコード直前）で定義
```

**この定義位置より前でトップレベル実行されるコードが `e(...)` を呼ぶと `ReferenceError` になり、スクリプト全体の初期化がそこで止まる。**

症状は「ステージ一覧が表示されない」「オプションが閉じられない」「記録ボタンが無反応」といった**一見無関係な複合不具合**として現れる。原因が1箇所のTDZだと気づきにくい。実際にこれで時間を溶かした。

- **対策**: 定義位置より前でDOMを触るなら `document.getElementById` を直接使う。
- 関数宣言の**内部**で `e()` を呼ぶのは問題ない（実行は起動後なので定義済み）。
- **UIに手を入れたら必ず `dev-tests/page_smoke_test.js` を通す**。これはこの種の不具合を検出するために作った。

### 4.2 重力整定の順序

盤面を組み立てたら必ず **`assignIds` → `gravitySettle3D`** の順。逆にすると連結グループが未確定のまま重力判定され、無関係なブロックが一括で落ちる。

```js
const w = Engine.makeWorld(W,H,D, st.walls===false);
for (const v of st.voxels) Engine.setVox(w, ...);
Engine.assignIds(w);        // ← 先
Engine.gravitySettle3D(w);  // ← 後
```

### 4.3 押せるブロックの識別子は圧縮で振り直される

`id` / `grp` は `compress` → `reconstruct` を通ると再割り当てされる。**状態の同一性判定に使ってはいけない**（ソルバーの状態キーは座標・種別・個数だけで作っている）。これを誤ると同じ盤面を無限に別状態として展開する。

### 4.4 2Dグリッドの高さプロパティは `H2`

3Dワールドは `H` だが、2Dグリッドは `W2` / `H2`。テストを書くとき `H` と書いて動かない、というミスを何度かした。

### 4.5 ステージ設計で最頻出のミス

**手前詰め（front-packing）の忘れ**。圧縮時、ブロックは畳む方向の手前に詰められる。これを忘れた配置は想定と違う2D盤面になる。エディタの4方向プレビューで必ず確認する。

### 4.6 テストが落ちたらまずテストを疑う

これまでの経験上、章別テストの失敗は**テスト側の記述ミス**（座標の思い違い、`H`/`H2` 混同、想定手順のズレ）であることが多かった。エンジンを直す前に、実際の盤面状態をダンプして確認する。

---

## 5. 作業手順

### 5.1 テストを走らせる

各テストは `dev-tests/_helper.js` を通じて本体HTMLを自力で読み込むので、事前準備は不要。
リポジトリルート・`dev-tests/` のどちらから実行しても動く。本体HTMLの名前は `index.html` でも
`dimension-puzzle-game.html` でもよい（ヘルパーが両方を探す）。

```bash
npm test          # ロジック層（DOM不要・高速）: ソルバー + スコア
npm run test:page # ページ起動層（要 npm i）: 起動スモーク + ヒント状態遷移
npm run test:all  # 全部

# 個別に
node dev-tests/solver_test.js   # ソルバー＋全47ステージが解けること
node dev-tests/score_test.js    # 星評価・par・ヒント探索
```

新しくテストを書くときはヘルパーを使う:

```js
const path=require('path');
const H=require(path.join(__dirname,'_helper.js'));
const M=H.loadModules();               // {Engine,Game,Game3D,FOLD,Solver,Score}
const st=H.loadStage('stage0902');     // ステージJSON
const w=H.worldFromStage(M.Engine,st); // assignIds→gravitySettle3D 済みのワールド
const rules=H.rulesFromStage(st);
const t=H.runner('テスト名'); t.add('項目', 条件); t.done();
```

`H.loadModules()` は本体HTMLの最後の `<script>` から `const Engine=` 〜 `/* ===== APP` を切り出して評価する。
**この2つの目印を消すと全テストが壊れる**ので注意。

### 5.2 ステージを追加・修正する

1. エディタ（`dp.dev(1)` → タイトルの「エディタ」）で編集
2. エディタの**「設計チェック」で、可否・リーク（折らずに解けないか）・近道を確認**
3. 「JSON書き出し」→ `stages/` に配置
4. `npm run build`（= `python3 build_stages.py`）で `stages.js` と `manifest.json` を更新
5. **par を再計算**: `npm run pars`（盤面を変えたら必須）
6. `npm test` で全ステージが解けることを再確認

ローカル確認は `python3 -m http.server` → `http://localhost:8000/index.html?dev`（`stages/` から直接読むのでビルド不要）。

### 5.3 ステージ設計時の検証（本編を追加する場合）

章別テストを書いて通す。骨子は仕様書 18.5 節を参照。要点は「想定解が通る」「折らずには解けないことをBFSで証明」「罠が意図どおり失敗する」の3点。

`bfs3Dpush`（4方向×歩く/押すの8手を全探索）は毎回テスト内に定義して使ってきた。ソルバーが実装済みなので、`Solver.search(node, rules, {foldless:true})` でも同じ証明ができる（こちらの方が簡潔）。

---

## 6. 設計上の重要な決定と、その理由

判断の背景を残す。変更する場合はここを踏まえて。

- **ソルバーは実エンジン関数を呼ぶ**。判定ロジックを二重実装しないため。「ソルバーが解けると言った手順は実プレイでも必ず同じ結果になる」ことが保証される。
- **「解なし」と「探索打ち切り」を必ず区別する**。混同すると設計判断を誤る（解けるステージを設計不良と誤認する）。UIも三値表示。
- **swap と詰み検出は既定オフ**。どちらも状態空間・計算量が跳ね上がるため。ただし swap オフで解が出ないときは「解なし（swapを除く探索）」と明示し、swap必須ステージを誤判定しない。
- **星は手数のみで決まる**。ヒント/アンドゥ未使用は別バッジ。評価軸を分離しておくと後から連動に変えるのも容易。
- **ヒント表示は「盤面が変化するまで持続」**。時間で消すと焦る、実行判定は実装が複雑。「動いたら消える」なら古いヒントが嘘にならず実装も単純。
- **ヒントの矢印はワールド座標に描く**。3Dの移動キーはカメラ相対なので、キー名だけ出すとカメラを回した瞬間に嘘になる。矢印なら常に正しい。キータグは `moveAxes()` の逆写像で現在のカメラから逆算している。
- **何も起きなかった入力は手数に数えず、アンドゥ履歴にも残さない**。壁に向かって連打してもペナルティも履歴汚染もない。
- **詰みは許容する**（取り返しのつかない一手を認める）。緊張感のため。だから詰み検出は「合否判定」ではなく「どの手が詰むかの可視化」として設計している。
- **ステージは手作り**。自動生成しない。ソルバーは生成ではなく検査のための道具。

---

## 7. 次に着手するときの推奨

未着手項目のうち、**B（吸収警告と敗因リプレイ）** が最も費用対効果が高い。既存の危険予測ハイライトとスナップショット基盤の延長で実装でき、「難しさは保ったまま理不尽さだけ取り除く」方向でこれまでの設計方針と一致する。

ただし**吸収警告は緊張感とのトレードオフがある**。8-3や9-4の「折る前に一歩ずれる」ドラマを削ぐ可能性があるため、実装前に方式（常時表示／オプション／一度死んだ後だけ表示）を決める必要がある。ユーザーは「Bを実装する際に決める」としている。

あわせて、**9-2など近道が見つかっているステージの扱い**（2章末尾の保留事項）も決めておくとよい。
