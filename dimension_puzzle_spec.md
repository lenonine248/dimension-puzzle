# 奥行キ消失点 — VANISHING DEPTH（次元圧縮パズル）設計仕様書 — v10

> 2次元と3次元を任意に行き来し、次元の切り替えで世界そのものが再構成されるパズルゲーム。
> v9 でチュートリアル（第1〜7章）・タイトル/メニュー・エディタ拡張・各種UI/演出を実装。
> **v10 で本編（第8〜14章）を完成させ全14章47ステージとし、ソルバー・スコア（par/星）・
> 次の一手ヒントの視覚化・記録（統計）・Nodeテスト基盤を追加した。**
> コアメカニクスに加え、実プレイに必要な周辺機能（進行・救済・見た目のフィードバック）も確定。

---

## 1. コンセプト概要

- プレイヤーは **3D（自由オービットカメラ）** と **2D（横視点の立面）** を任意に切り替えられる。
- 重要なのは「3D空間にカメラを置いて2Dっぽく見せる」のではなく、**次元を切り替えるたびに世界の情報そのものが再構成される**こと。
- 3D→2D では奥行き方向の情報が圧縮され、**不可逆に失われる**。この情報損失こそがパズルの核。
- プレイヤーのアバターも「ブロックの一種」として同じルールに従う。これにより、アバターの **消滅・増殖** が自然に発生する。
- ゴールは「アバターをゴールエリアに到達させること」。
- 単一HTMLファイル・依存なしで動作し、GitHub Pages 等で配布する。ステージデータは外部JSONとして分離し、`build_stages.py` でビルド時にHTMLへ同梱する。

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| 列（カラム） | 奥行き（畳む軸）方向に並ぶブロックの並び。2Dの1セルに対応する。 |
| 圧縮 | 3D→2D変換。各列を `{種類, 個数}` に畳む。 |
| 再構成 | 2D→3D変換。各セルの `{種類, 個数}` から3Dの列を復元する。 |
| 最前面 | カメラに最も近いブロック。圧縮時に「種類」として保存される基準。 |
| 個数 | その列のブロック数。2Dでは数字として表示・保持される。 |
| 均質化 | 往復によって、列の全ブロックが最前面の種類に上書きされる現象。 |
| 押す | アバターが「押せるブロック」を隣のマスへ移動させる操作（Shift＋移動、2D/3D両対応）。 |
| 登る | アバターが障害物・押せるブロック・他のアバターの上へ1段上がる操作（修飾なし移動）。 |
| 連結グループ | 同一識別子を持つ押せるブロックの集合。押しても重力でも1つの剛体として動く。 |
| 章（チャプター） | ステージのティア（`tier`）に対応する学習単位。`stages/tiers.json` で章名を持つ。 |
| 壁なし（open） | ステージ周囲・天井に壁が無い設定。境界の外は「何もない場所」と同じ扱いになる。 |
| 奈落 | Y=0に支えの無い列。重力ブロック・アバターが支えを失うと落下・消滅する。 |

---

## 3. 座標系と次元の対応

```
        y（高さ）
        ↑
        |
        +────────→ x（横）
       /
      /
     z（奥行き／カメラ視線方向）
   ↙ カメラ
```

- **3軸**: x（横）, y（高さ）, z（奥行き）。
- **3Dカメラは自由オービット**（ドラッグで回り込み、ホイールでズーム）。カメラは観察専用で、ブロックや畳みの論理には影響しない。投影は正射影ベース。
- **畳む向きは4通りの離散選択**（±x / ±z の4面のいずれか。高さ y は決して畳まない）。選び方は **ハイブリッド方式**:
  - カメラの水平向きに最も近い面を **既定の畳み面としてハイライト**（カメラアシスト）。
  - プレイヤーはギズモ（4方向の矢印）やSpaceキーで **明示的に上書き選択** できる（同じ矢印を再度押すと選択解除＝カメラ追従に戻る）。固定中の向きは追従時より強くハイライトして区別する。
  - 選択中の向きで畳んだ結果（プレイヤーが見る2D立面）を右側に **ライブプレビュー** 表示。
  - 確定すると2Dへ。
- **最前面（保存される側）はカメラ位置に依存せず、選んだ畳む向きの「手前端」で決定論的に定まる**。例：+z 面を選べば z が大きい側が最前面、再構成はそこから −z へ伸ばす。これによりカメラがどの角度でも結果が一意になる。
- **2D立面は畳む向きごとの正準（canonical）な側面図**として、向きごとに固定の規則で左右を決める。
- 同じ3D状態でも、選ぶ向きで **最大4通り**の異なる2Dが得られる。
- 畳む向きの選択はブロックを動かさない。あくまで「次に畳む向きの指定」。
- **畳む向きはステージごとに個別制限できる**（`foldAllow`）。不許可の向きは床の矢印が非表示になり選べない。**全方向を不許可にすると、そのステージは一切2Dへ圧縮できない3D専用パズルになる**（次元切り替えボタン・Spaceキーが無効化され、読み上げに「圧縮不可（3D専用ステージ）」と表示される）。

---

## 4. ブロックの種類

すべての種類に「重力あり／なし」のバリエーションがある（**アバターを除く**）。3D・2D・エディタのいずれの表示でも、重力ありのブロックには小さな下向き矢印（重力マーカー）が付き、重力なしには何も付かない。

| 種類 | 数字の入れ替え | アバターが押せる | 当たり判定 | 重力 |
|---|---|---|---|---|
| アバター | 可 | （押す側であり対象外） | 他アバターとは重ならない／壁・障害物で停止 | **常に重力あり（固定）** |
| 地面 | 可 | **不可** | 上に乗れる・側面は通れない | あり／なし |
| 押せるブロック | 可 | **可（2D・3D両方）** | 上に乗れる・側面は通れない | あり／なし |
| ゴールエリア | 可 | 不可 | アバターが進入で **クリア** | あり／なし |
| 障害物 | 可 | 不可 | 触れるとそのアバターが消滅 | あり／なし |

- **地面**: 数字の入れ替えは可能だが、2D・3Dどちらでも **アバターが押して動かすことはできない**。
- **押せるブロック**: 数字の入れ替えに加え、2D・3Dどちらでも **アバターが押して動かせる**（→ 9章）。
- **ゴール・障害物**: 数字の入れ替えは **常に可能**。プレイヤーが能動的にゴールの個数を減らして消滅させる／障害物を増やすことも起こりうるため、**それを前提にステージを設計する**。
- 重力なし＝空中に固定。重力あり＝支えがないと落下（例：落ちる足場）。

---

## 5. 次元変換のルール（コアメカニクス）

### 5.1 圧縮（3D → 2D）

各セル（奥行き方向の列）を2つの情報に畳む。

- **種類** = その列の **最前面（カメラ側）ブロックの種類**（重力バリエーションも継承）
- **個数** = その列のブロック数
- 空の列 → 個数0・種類なし

### 5.2 再構成（2D → 3D）

各セルの `{種類, 個数}` から、**最前面を起点に、奥へ向かって個数ぶん**ブロックを並べる。

- 種類・重力バリエーションはセルのもの（＝元の最前面ブロック）を継承。
- 押せるブロックが列内に連続してできた場合、その並びは新しい1つの連結グループ（新識別子）とみなされる（→9.4）。

### 5.3 均質化（情報の不可逆な損失）

```
【3D・1つの列を真横から（z = 奥行き）】
カメラ →  z0    z1    z2
         [動]  [地]  [地]      最前面 = z0「押せるブロック」／個数 = 3

【2Dへ圧縮】  →  セル { 種類:押せる, 個数:3 }

【3Dへ再構成】 最前面起点で奥へ
          z0    z1    z2
         [動]  [動]  [動]      全部「押せる」に上書き（奥の地面は消失）
```

「どの向きから畳むか」によって何が残り何が消えるかを設計できる。これがパズルの核。

---

## 6. 数字（個数）の入れ替えルール

- 2Dでのみ実行可能。
- **入れ替えるのは「数字（個数）」だけ**。種類はその場に残る。
- **入れ替えは「アバター ↔ そのアバターに隣接するセル」の間でのみ可能**。すなわち入れ替える2セルの一方は必ずアバターで、もう一方はそのアバターに隣接する非空セル。
- 隣接は **縦・横のみ**（斜め不可）。
- **空セル（個数0）とは入れ替え不可**。
- **1手で1入れ替えのみ**（連鎖・複数同時は不可）。
- 相手側の対象はゴール・障害物・地面・押せるブロック・他アバターを含む全種類（ただしアバターに隣接していること）。
- **複数アバターがいる場合は、入れ替えを行うアバターを1体選択**してから、その隣接セルと交換する（全アバター一斉ではない）。
- **ステージ単位で入れ替え自体を禁止できる**（`swapAllow:false`）。禁止時はアバターをクリックしても選択・交換が起きない（見た目・レイアウトは変えない）。

> 設計意図: 操作をアバターの位置に局所化し、移動と数字操作を一体化する。到達できる範囲しか操作できないため、移動と操作が相互に依存するパズルを作れる（詳細は旧設計メモの「6.1 ねらい」を参照。要旨：操作の局所化、数字＝ペイロード、運搬と受け渡し、到達性ゲート、リスク、設計制御）。

---

## 7. 重力ルール

- **重力は次元切り替えの両方向で適用する**。2Dへ圧縮した直後は列（セル）単位、3Dへ再構成した直後は(x,z)列ごとにy方向へ、それぞれ落下整定する（重力あり・非アバターのみ対象。アバターは支えとして扱う）。
- 解決タイミング：次元切り替え時に加え、押し操作後、数字入れ替え後など、ワールド状態が変化したとき。いずれも **支えに着地するまで一気に**落下させる。
- **重力ありブロックは、支え（下に何か存在すること）がないままY=0まで落ちると、アバターと同様に場外へ消滅する。** ワールドの最下段（Y=0）は暗黙の床ではなく、地面など非重力の静的なブロックを明示的に置いた場所だけが支えになる。
- **連結グループ（同一識別子）は3D中、重力についても1つの剛体として扱う。** グループ内のどこか1点でも（グループ外の何かに）支えられていれば、グループ全体は不動（一部だけ穴の上に来ても、その部分だけが消えることはない）。グループのどこにも支えが無い場合は、全体が支えの見つかる高さまで揃って落下し、それでも支えが得られなければグループ全体が同時に場外消滅する。グループ内に重力なしの部材が混在する場合は、その静的な部材に引きずられてグループ全体が不動になる。
- アバターは常に重力あり。歩行（2D・3D共通）では、移動した直後にその場で**支えに着地するまで一気に**落下する。ゴールへ落ちればクリア、障害物へ落ちれば消滅。次元切り替え時も同様にアバターの重力を解決する。
- **落下死の境界**: ステージの **Y座標0より下**に落ちたものは失われる（消滅する）。
- **壁なし（`walls:false`）のステージでは、周囲・天井の境界そのものが「何もない場所」として扱われる。** その方向への歩行・押し・登りは妨げられず、進んだ先に支えが無ければ、内部の穴と同じ規則で落下・消滅する（天井側は登り続けられる＝高さ上限が外れる）。3D画面では境界の外側にも縁と霧が表示され、危険ハイライトも境界の外まで働く。既定（`walls`省略）は壁あり。

---

## 8. アバター（プレイヤー）

アバターはブロックの一種であり、同じ変換ルールに従う。

### 8.1 生存・消滅・増殖

- 圧縮時、自分の列で **最前面にいれば生き残る**。手前を他ブロックに塞がれていると、再構成で上書きされて **消滅**する。
- 列の個数が2以上になっていれば、再構成で **アバターが増殖**する。
- アバターの数字（個数）も2Dで入れ替え可能なので、入れ替えによる増殖・削減も起こりうる。

### 8.2 複数アバターの操作

- 全アバターは **同じ入力で同時に動く**（個別操作は不可）。
- 移動時、**一部だけが壁・端・障害物で進めない場合、進めるアバターだけが動く**（位置がずれていく）。
- 2体が同じマスに入ろうとした場合、**その方向には進めない**（重ならない）。

### 8.3 移動と登り

- アバターは2D・3Dの **両方で歩行可能**。3Dでは奥行き(z)方向にも歩ける。
- **修飾なしの移動は「登る」専用**：進行方向が地面・押せるブロック・他のアバターで塞がれていて、かつその1段上が空いていれば、そこへ1段登る（押さない）。登れなければ何もしない。
- **1回の移動で登れるのは常に1段まで**。ただし、段差が常に1段ずつであれば、階段状の配置を繰り返し登ることで制限なく高い場所へ到達できる（各段が独立に支えを判定するため）。足場は地面・押せるブロック・他のアバターのいずれでもよい（統一ルール）。他のアバターを足場にする場合、そのアバターが動けずその場に留まっている必要がある（全アバター同時移動のため、動けるアバターは通常どおり移動する）。
- 自分を列の最前面に持っていくことで、圧縮時の生存をコントロールできる。

---

## 9. ブロックを押す操作

押せるのは **「押せるブロック」のみ**。地面・他アバター・ゴール・障害物は押せない。2D・3Dの両方で可能。

### 9.1 操作方式（Shift＝押す、修飾なし＝登る）

- **修飾なしの移動＝登る専用**（8.3参照）。押さない。
- **Shiftを押しながらの移動＝押すことを優先**。目の前に押せるブロックがあり、押す方向の1マス先が空いていれば、そのブロックを1マス押す。
- **押せなかった場合は「登る・歩く」にフォールバックする**（8.3と同じ判定）。押す対象が無い・押す先が塞がっている・境界に達している等で押せない場合、代わりに登れれば登り、平地やゴール・障害物ならそのまま歩いて進入する。**それも不可能なら何もしない**（3段階フォールバック：押す→登る・歩く→何もしない）。
- **連鎖押しはできない**。押す方向のすぐ先に押せないブロックや別の押せるブロックがあると、そのブロック自体は動かない（＝上記フォールバックへ）。
- 押す方向は **水平（±x / ±z）**。3Dではz方向にも押せ、最前面の位置が変わるため次の圧縮結果に影響しうる。
- 重力ありの押せるブロックを押して支えを失った場合は落下する（7章）。壁なしステージでは境界の外へ押し出すこともでき、その場合は奈落へ落ちたのと同じ規則で消滅する。

### 9.2 上に載ったブロックの連動（縦方向キャリー）

- 押せるブロックの上に**重力ありの**押せるブロックが載っている場合、下を押すと上も連動して一緒に動く。
- **重力なしのブロックが上に載っている場合は追従しない**（その場に残る）。重力なしは支えを必要としないため、連動させる必要がないという判断による。
- 追従の判定は**何段でも再帰的**に行われる。押すブロックの真上に重力ありのブロックがあれば追従対象に加え、そのブロックのさらに真上に重力ありのブロックがあれば、それも加える……という形で、支柱状に何段積まれていても最後まで追従する。途中に重力なしのブロックが挟まると、そこで追従の連鎖が止まる（その上に重力ありが積まれていても、支えである重力なしブロック自体が動かない以上、その上も動かない）。
- 下は動けても、追従対象の移動先が塞がっている場合は、**下（押しているブロック）はそのまま動くが、移動先が塞がっているブロックとそれより上に載っているブロックは追従できず、その場に置き去りになる**。置き去りになったブロックは、下が抜けて支えを失えば重力で落下する（落下先で再度押せる）。ある段が置き去りになると、その上に載っていた段も（支えが動かない以上）連動して置き去りになる。下から順に、移動先が空いている限り追従し、最初に移動先が塞がった段とそれ以降（上）が置き去りになる。
- ただし、**下（押しているブロック）自身の移動先が塞がっている場合は、押し自体が失敗し何も動かない**（→ 9.1のフォールバックへ）。

```
押す方向 →
   [上]（重力あり）              [上]※中段が動かないので上段も置き去り
   [中]（重力あり）  ──▶  [中]※移動先が塞がっている→置き去り→落下
   [下] □                [下]（下だけ移動）
（先が空：全段が一緒に移動）      （下段のみ移動、中段・上段は残る）
```

### 9.3 長さのある押せるブロック・連結グループ

- 奥行きのある（個数2以上の）押せるブロックや、複数ボクセルからなる連結グループは、**1つの塊（一体）**として扱う。
- 押すと **塊全体が1マスまとめて動く**。x方向にも z方向にも押せる。
- 塊のどこか一部でも移動先が塞がっていれば、**全体が動かない**（部分的な移動はしない）。

### 9.4 ブロックの同一性（識別子・連結グループ）

- **3D中（再構成をはさまない間）は、各押せるブロックは固有の識別子を持つ独立したオブジェクト**。押すとその識別子の全ボクセルが一体で動き、別識別子のブロックとは隣接しても連鎖押しされない。
- **連結はステージ作者が明示指定する**。ステージJSONの押せるブロックは `[x, y, z, "push", gravity, group]` の第6要素 `group` で連結グループを指定でき、同じ `group` 番号の押せるブロックは同一識別子（一体）になる。`group` 省略時は後方互換として、隣接する省略ブロック同士を連結成分とみなす（旧式データ向け）。
  - エディタで新しく置いた押せるブロックは既定で単体（別グループ）。「連結ツール」で面隣接する2つをトグルし、任意形状のグループを作れる。
  - **エディタのJSON書き出しでは、単体グループは互換のため`group`を省略するが、隣接する押せるブロックがある単体は誤って自動連結されないよう`group`を明示的に出力する**（隣接する単体同士が読込時に意図せず連結される不具合の対策）。
- **圧縮（3D→2D）で識別子は失われる**。2Dに残るのは列ごとの `{最前面の種類, 個数}` だけ。
- **再構成（2D→3D）では、各列にできた押せるブロックの連続した並びを、それぞれ「1つの新しいブロック」とみなす**（新しい識別子を割り当てる）。この帰結として、隣接して置かれた別個の押せるブロックも、圧縮→再構成を経ると1つの塊に融合しうる。

---

## 10. 操作一覧

**タイトル画面**
- ステージモード（→ステージ選択画面へ）／オプション／エディターモード（開発者モード時のみ）

**ステージ選択画面**
- 章（tier）ごとに折りたたみ可能な一覧。章見出しに「クリア数 / 総数」を表示し、全クリア済みの章は緑色になる。
- 初回表示時は、次に遊ぶステージ（解放済み・未クリアの最初のステージ）が属する章だけが開いた状態になり、それ以外は閉じている。以後はユーザーの開閉操作を保持する。
- クリア済み(✓)・選択可能(▶)・未解放(🔒) をアイコンで表示。未解放はクリックしても反応しない。

**3D（自由オービットカメラ）**
- カメラ操作（ドラッグ回転・ホイールズーム）
- 修飾なし移動＝登る（WASD／カーソルキー）
- Shift＋移動＝押す（押せなければ登る・歩くにフォールバック）
- 畳む向きの選択（床の矢印：カメラ追従／クリックで固定・トグル解除）
- Spaceキーまたはボタンで2Dへ切り替え（圧縮）

**2D（横視点）**
- 修飾なし移動（←/→）＝登る
- Shift＋移動＝押す（同様にフォールバックあり）
- 数字の入れ替え（アバターを選択 → 隣接セルと交換。1手1入れ替え）
- Spaceキーまたはボタンで3Dへ切り替え（再構成）

**共通UI**
- U＝ひとつ戻す（アンドゥ）、R＝最初に戻す（リセット）
- 画面右上の丸いアイコンボタン（⚙）でオプションウィンドウを開く（背景を暗くして中央に表示、下部の閉じるボタンで閉じる）。タイトル・ゲーム内で同じ挙動に統一。
- ステージにヒントがある場合、盤面上部に短いヒント文を表示。右上の×で閉じられ、ツールバーの「ヒント」ボタンで再表示・再非表示を切り替えられる（ヒントの無いステージではボタンが無効化される）。
- ステージクリア時は、次のステージがあれば「次のステージへ」ボタン、最後のステージ（または進行文脈が無い場合）は「タイトルへ」ボタンを表示する。
- ツールバーの「ステージセレクトへ」ボタンでいつでもステージ選択画面に戻れる。

---

## 11. 状態遷移

```
  タイトル ⇄ ステージ選択 ⇄ ゲーム（3D/2D） ⇄（開発者モードのみ）エディタ／テストプレイ
```

ゲーム画面内では：

```
        ┌──────────────────────────────────┐
        │        3D 自由オービットカメラ      │
        │ 歩行・登る / 押す / カメラ回転      │
        └────────────────┬─────────────────┘
            ▲            │ 2Dへ（圧縮、Space）
   3Dへ     │            ▼
 （再構成、Space）│   ┌──────────────────────────────────┐
            └───│            2D 横視点               │
                │ 歩行・登る / 押す / 数字入替        │
                └──────────────────────────────────┘
```

---

## 12. 勝敗条件

- **クリア**: アバターが **1体でも**ゴールエリアに進入する。クリア時、そのステージのクリア記録が保存され、次のステージが解放される。
- **ゲームオーバー**:
  - アバターの総数が **0** になる（消滅・落下死などで全滅）。
  - **ゴールエリアが全て消滅**する（最前面ルールや数字入れ替えで失われ得るため）。
- どちらもUndo（U）またはリセット（R）で立て直せる。

---

## 13. ステージ進行・解放・チュートリアル

### 13.1 構成

- ステージは `tier`（章番号）と `order`（章内の並び順）を持ち、`stageOrder` は全ステージを `order` の昇順に並べた1本のリストとして扱う。
- **序盤ティア（第1〜7章）はチュートリアルとして設計されており、要素を1つずつ段階的に導入する。**
  1. **第1章「圧縮」**（歩く・畳む・再構成の往復）
  2. **第2章「情報」**（数字＝奥行きの個数、畳む向きで結果が変わる）
  3. **第3章「交換」**（数字入れ替え、アバター増殖・削減）
  4. **第4章「押し」**（Shift押し、連結ブロックの一体移動、上載せ運搬）
  5. **第5章「重力」**（重力ブロックの落下・奈落消滅、剛体の張り出し、落下配達）
  6. **第6章「群体」**（複数アバターの同時移動・階段登り、障害物）
  7. **第7章「制約」**（畳む向き制限・3D専用・入れ替え禁止・壁なし、応用）
- 各ステージは、そのステージで初めて登場する要素だけを教えるよう最小限の盤面で設計し、想定解をNode上のシミュレーションで検証してから配置する。
- **本編（第8〜14章）はチュートリアルで教えた全要素を「素材」として、その組み合わせと裏返しで構成する。** 各章に核となる発見を1つ置き、章内で深掘りする。全ステージを、押し込み対応の総当たり探索（BFS）で「折らずには解けない（あるいは必要な体数でしか解けない）」ことを機械的に検証してから配置している。取り返しのつかない一手（Undo前提の詰み）も緊張感を作るために許容する。
  8. **第8章「交差」**（二軸の折り畳み。±x折りの手前詰め・雪崩・軸の交互利用・吸収の負け筋）
  9. **第9章「上書キ」**（均質化の武器化。障害物の消毒・壁の軟化・荷物の凍結・ゴールの防御）
  10. **第10章「複製」**（材料の増産と融合。数字を運んで押せる物を増築・単体の融合・一点支持の石橋）
  11. **第11章「落チル数字」**（2D側の物理。2D内で押して落とす・積み替え・二段落チによる櫓）
  12. **第12章「行進」**（群体の運用。溝を埋める礎・二段の櫓・必要な体数の逆算。BFSで最小体数を保証）
  13. **第13章「果テノ外」**（開世界の応用。天井なしの高さ超え・虚空に架ける桟橋・果てへ塔を捨てる）
  14. **第14章「消失点」**（総合。ゴールの均質化拡散・軟化と増築の複合・消毒と櫓でタイトルを回収）
- 章名は `stages/tiers.json`（`{"1":"圧縮", ...}`）で管理し、`build_stages.py` が `window.TIERS` として `stages.js` に同梱する。定義が無い章は「TIER n」表示にフォールバックする。

### 13.2 解放ルール

- **連続解放のみ**: ステージ n をクリアすると、`stageOrder` 上で次のステージが解放される（先取り解放は採用していない）。
- 解放状況・クリア状況は `localStorage`（`dp_progress`）に保存される。

### 13.3 ヒント

- ステージJSONの任意フィールド `hint`（文字列）に短いチュートリアル文を書ける。値のあるステージだけ盤面上部にヒントバーが表示される（10章参照）。
- エディタの操作バーに「ヒント」入力欄があり、テストプレイでの確認・JSON書き出し（空なら省略）に対応する。

### 13.4 難易度の軸（ステージ設計の指針）

第8章以降（本編）のステージ設計では、次の軸を単独／複合で増減して難易度を作る。

- **規模**: グリッドサイズ、ブロック数。
- **手数（解の深さ）**: 次元切替・数字入れ替え・押しの回数。
- **軸選択**: 正解の畳む向きを見つけさせるか。
- **均質化の活用と回避**: 列に積む種類数。狙って消す/変えるか、守るか。
- **数字入れ替えの絡み**: 入れ替え回数、動線、入れ替え→重力落下の連鎖。
- **重力**: 重力あり/なしの混在、支え構造、カスケード落下、連結グループの張り出し。
- **アバター**: 生存・増殖・複数同時操作・必要数・階段登り。
- **押し**: 位置合わせ、z方向に押して最前面を作り替える応用、上載せ連動、穴への落下。
- **ゴール・障害物の脆さ**: 守る／動かす／入れ替えで動かす。
- **ステージ別ルール**: 畳む向き制限・3D専用・入れ替え禁止・壁なしの組み合わせ。
- **取り返しのつかなさ**: 一手で詰む配置を含めるか。

導入順の一例（本編）: 均質化 → 軸選択 → アバター生存 → 数字入れ替えの応用 → 押し → アバター増殖 → 複数アバター同期 → 障害物 → 複合。

---

## 14. 体験設計・リカバリ

- **アンドゥは操作単位（実装済み）**: 移動・登る・押す・数字入れ替え・次元切り替えのいずれの操作の直前にも状態のスナップショットを取る（最大300件保持）。Uキーで1手ずつ遡れる。歩行中の重力落下やFXそのものは巻き戻しの単位ではなく、「その操作をする前」まで戻る。
- **リセット（R）** でステージの最初からやり直せる。
- ゲームオーバー時も同じUndo/Resetの導線で立て直せる。

### スコア（手数と星評価）

- 歩く・登る・押す・畳む・展開・数字入れ替えを、それぞれ**1手**として数える（何も起きなかった入力は数えず、アンドゥ履歴にも残らない）。プレイ中は画面上部の読み上げに「手数 N / 最短 M」を常時表示する。
- 各ステージは、ビルド時に全操作（swap込み）のソルバーで求めた**最短手数（par）**をJSONに持つ（`dev-tests/compute_pars.js` で一括計算・焼き込み）。
- クリア時に星評価を表示・保存する: **★3＝par以内 / ★2＝par+max(2, ceil(par×0.5))以内 / ★1＝クリア**。星は純粋に手数のみで決まる。
- **実績バッジ**: 「アンドゥなし」「ヒントなし」でのクリアを星とは別に記録する。ヒント使用はアンドゥしても取り消されない。ステージ選択画面にはクリア済みステージの最高星が表示される。
- ベスト記録（最高星・最小手数・各バッジ）は `localStorage`（`dp_progress` の `results`）に保存され、良い方だけが更新される。

### 次の一手ヒント（視覚化）

- ツールバーの「次の一手」ボタンで、**現在の盤面**からソルバー（swap込み）が最短解を計算し、**最初の1手だけ**を盤面上に視覚的に提示する。
- 表示方法は手の種類ごとに異なる:
  - **歩く/登る**: 全アバターの頭上に進行方向の矢印。
  - **押す**: 進行方向の矢印＋押す対象ブロックの輪郭ハイライト。
  - **畳む**: 該当方向のフォールドギズモを発光。
  - **展開**: 「次元を切り替え」ボタンを発光。
  - **数字入れ替え**: 2Dの該当2セルを枠で強調し、間に「⇄」を描画。
- 矢印は**ワールド/グリッド座標に直接描く**ため、カメラをどう回しても正しい方向を指す。あわせて操作キーのタグ（現在のカメラでの W/A/S/D、Shift、Space など）を添える。3Dの移動キーはカメラ相対のため、カメラ角度から実際に押すべきキーを逆算して表示する。
- **表示は盤面が変化するまで持続する**。ヒントの手を実行しても、別の実効手を打っても、盤面が1手でも動けば消える（古いヒントが嘘にならない）。壁に向かうなど盤面が変化しない入力ではヒントは残る。
- 現在の盤面からもう解けない場合／探索が上限で打ち切られた場合は、矢印を描けないため従来どおりトーストのテキストで「もう解けません…」「複雑すぎて計算できませんでした（未確定）」と区別表示する。
- アニメーション設定OFF時は、パルス／点滅を静止表示に落とす。
- 使用回数は試行ごとに記録され、「ヒントなし」バッジの判定に使う（星評価には影響しない）。

### 記録（統計）

- タイトルメニューの「記録」から、累計統計を閲覧できる: クリア数・星の合計・アンドゥなし/ヒントなしクリア数・総手数・畳んだ/展開した回数・押した回数・数字入れ替え回数・アンドゥ回数・ヒント回数、そして**「消失点に消えたもの」**（奈落や均質化で失われたブロック数・体数）。
- 統計は `localStorage`（`dp_stats`）に保存される。

---

## 15. ゲームプレイの核となるループ

1. 3Dで地形とアバターの位置関係を確認する。
2. カメラを回転させ、**どの向きから畳むか**を選ぶ（＝何を最前面=保存対象にするか）。必要なら押せるブロックを **押して** z位置を調整する。
3. 2Dに圧縮し、**数字を入れ替え**て地形の奥行きを作り替える。
4. 3Dに再構成すると、均質化と奥行きの変化で地形が一変する。
5. アバターを歩かせ／登らせ／押させ／増やし／生存させながら、ゴールへ導く。

「どの軸で畳むか × 何を最前面にするか × どの数字を入れ替えるか × どこで何を押すか」の組み合わせが解法空間になる。

---

## 16. 視覚フィードバック（確定・実装済み）

プレイヤーが「何が危険か」「何が重力に従うか」を常に把握できるよう、以下の常時／条件付き表示を設けている。

- **危険ハイライト**: 各アバターについて、その場から歩く・押すことで次の1手が奈落（Y<0）に落ちて消滅する方向を常時計算し、3Dは床への破線枠＋下向き矢印、2Dは破線枠で警告表示する。アニメーション設定がオンなら点滅、オフなら静的表示。
- **奈落の縁と霧**: Y=0に支えの無い列（穴）の境界に縁ラインを描き、その下へ青白く暗くフェードする霧を表示する。3Dでは地面側（内側）から見たときだけ霧が見え、反対側からは見えない（面の向きに応じたカリング）。壁なしステージでは盤の外周にも同様の縁と霧が表示される。2Dは最下段の下にのみ表示。
- **重力マーカー**: 重力ありのブロック・アバターには小さな下向き矢印を表示する（3D・2D・エディタ共通）。重力なしには何も表示しない。
- **落下・消滅の演出**: 場外へ落ちて消えるブロック・アバターは、青白い残像トレイルを引きながら落下し、奈落ラインに到達した瞬間に青白い破片が散る。障害物に触れて死ぬ場合は赤いバーストになり、死因を色で区別できる。演出はアニメーション設定に従い、オフ時は即消滅する。
- **ブロックの縁線**: 3D（プレイ・エディタとも）で、連結していないブロック同士の境界には濃さ0.42のはっきりした縁線を引き、切れ目を分かりやすくしている（連結グループの内部境界には引かない）。

---

## 17. タイトル・メニュー・演出（確定・実装済み）

- **タイトル画面**: タイトル名「奥行キ消失点 — VANISHING DEPTH —」（副題：次元圧縮パズル）。背景に、消失点へ収束する透視線と同心円がゆっくり動く演出（アニメ設定オフ時は静止画）。メニューは「ステージモード」「オプション」、開発者モード時のみ「エディターモード」。
- **オプションウィンドウ**: タイトル・ゲーム内どちらから開いても同じ挙動（背景を暗くするオーバーレイ＋中央のカード、下部に閉じるボタン）。サウンドのオン/オフ・音量・アニメーション演出・画面の揺れを設定できる。
- **BGM/SE**: WebAudio合成によるループ2曲（タイトル/選択画面＝浮遊感のあるパッドの分散和音＋低音ドローン、ゲーム中＝まばらな低音パルス＋高音の瞬き）。エディタ中は無音。メニュー操作・押す・登る・クリア等の効果音は既存のSEを使用する。ブラウザの自動再生制限のため、最初のクリックまたはキー入力からBGMが鳴り始める。
- **開発者モード**: ブラウザのコンソールで `dp.dev(1)` を実行すると有効化（`localStorage` に永続、`dp.dev(0)` で無効化）。有効時のみ、タイトルメニューの「エディターモード」・ゲーム内ツールバーの「エディタ」ボタン・ヘッダーの「DEV MODE」バッジが表示される。

---

## 18. 開発者用ステージエディタ（確定・実装済み）

本作はランダム自動生成が難しいため、ステージは手作りする。そのための開発者用エディタを用意している。

**構成**
- **本体HTML内の開発者モード**として実装（`dp.dev(1)` で有効化）。ゲーム本体と同じエンジン・同じルールで編集・即テストでき、ルールのズレが起きない。配布版ではエディタUIを出さない。
- **編集ビューは2種類を併用**:
  - **3D直接配置**: クォータービューでボクセルを直接置く/消す。**Shiftキーを押している間はカメラ操作が止まり、既存ブロックの面をクリックするとその面の外側にパレット選択で新規配置**（既に何かある位置には置かない）、**消しゴム選択中はクリックしたブロック自体を消去**する（スライス編集グリッドにも即座に反映される）。
  - **平面編集（スライス）**: 高さ(y)層ごと、または奥行き(z)スライスごとの2D平面で置く/消す。クリックで配置／消去。
- ツール: ブロック種類（地面/押せる/ゴール/障害物/アバター）の選択、重力あり/なし切替、配置・消去、グリッド範囲(x,y,z)設定、ステージ番号・ティア割り当て、アバター初期位置、ヒント文入力。
- **連結ツール**: 押せるブロックは既定で単体。「連結ツール」を選び、面隣接する2つの押せるブロックを順にクリックすると連結／分割をトグルする（未連結なら2つのグループを統合、連結済みならそのグループ全体を単体化）。連結グループはスライス上で青い輪郭、3Dプレビューで一体（内部境界線なし）として表示される。
- **ステージ別ルール設定**: 畳む向き（+x/-x/+z/-z）を個別に使用可/不可（`foldAllow`）、数字入れ替えの可/不可（`swapAllow`）、周囲・天井の壁の有無（`walls`）を、それぞれチェックボックスで設定できる。全方向不許可は3D専用ステージとして正当にサポートされる。既定値のときはJSONに出力されない（省略＝互換）。
- **4軸ぶんの2Dプレビュー**: 各畳む方向で圧縮した結果を一覧表示し、設計意図どおりか即確認できる。
- **その場プレイテスト**: 編集↔テストプレイを切り替えて手動で解けるか確認する。テストプレイ→編集に戻る→プレイに戻る、という往復をしても、最初に選んでいたステージへ正しく復帰する（エディタに入る前の選択ステージIDを保持し、テストプレイでの一時的なクリアによる上書きから復元する）。
- **書き出し／読み込み**: 3DグリッドをJSONにシリアライズしてダウンロード、または既存JSONを読み込んで編集を再開できる。

**ソルバー**
- `solver_spec.md`（draft v1）に準拠したソルバーを実装済み。状態探索（BFS）による可否判定・最短手数(par)・解パスの提示、リーク検出（折らずに解けるか）、近道検出（想定解との手数比較）を既定機能として持つ。数字入れ替え（swap）を探索に含めるオプションと、詰み検出（デッドロック・危険手・即死手の列挙）オプションは、いずれも計算コストが増すため既定オフの任意機能。
- 実エンジン関数（`Engine`／`Game`／`Game3D`のcompress/reconstruct/move/gravitySettle系）をクローン盤面上でそのまま呼び出す方式で、判定ロジックを二重実装しない。状態キーは座標・種別（2Dは個数も）で正規化し、圧縮→再構成で振り直される内部識別子（id/grp）はキーに含めない。
- エディタパネルの「解を探す」「設計チェック」「解を再生」「想定解の記録」から利用する。探索は状態数上限・時間上限で打ち切り可能で、打ち切り時は「解なし」と区別して「未確定」を返す。詳細は `solver_spec.md` を参照。

---

## 18.5 開発・テスト基盤（確定・実装済み）

ブラウザ実行のゲームだが、ロジックはNode上で自動テストしている。テストは `dev-tests/` に置く。

**二層のテスト**

1. **ロジック層（Node直接）**: 本体HTMLの最後の `<script>` ブロックから、`const Engine=` 〜 `/* ===== APP` を切り出して評価し、`Engine` / `Game` / `Game3D` / `FOLD` / `Solver` / `Score` を取り出す（`dev-tests/_helper.js` の `loadModules()` が担当）。DOMもCanvasも不要で高速。ステージの解法検証・エンジンの物理検証・ソルバー・スコアはすべてこの層で行う。**この2つの目印を消すと全テストが壊れる。**
2. **ページ起動層（jsdom）**: Canvas・AudioContext をモックして実際にページを起動し、ボタンをクリックする。**ロジック層では検出できない初期化エラー（後述のTDZ由来など）やボタン未配線を検出する**ためにある。`dev-tests/page_smoke_test.js`・`dev-tests/hint_visual_test.js` がこれにあたる（`npm i jsdom` が必要）。

**恒久テスト（`dev-tests/`）**

- `solver_test.js` … ソルバーの可否判定・リーク検出・打ち切り区別・詰み検出、および**全47ステージがソルバーで解けること**。
- `score_test.js` … 星評価の閾値、全ステージのpar存在、2D途中盤面からのヒント探索。
- `page_smoke_test.js` … 起動エラーなし・ステージ一覧描画・オプション/記録の開閉・ステージ開始（jsdom）。
- `hint_visual_test.js` … 次の一手ヒントの状態遷移（設定→実効手で消去→非実効入力では残存）（jsdom）。
- `compute_pars.js` … テストではなくツール。全ステージにソルバーを走らせ `par` をJSONへ焼き込む（`npm run pars`）。
- `_helper.js` … 共通ヘルパー。本体HTMLの探索、モジュール抽出、ステージ読み込み、簡易テストランナー。各テストはこれ経由で自己完結して動く（事前のファイル抽出は不要）。

**ステージ検証の定型（章別テスト）**

各章のステージ設計時には、その章専用の検証スクリプトを書いて通す運用をしている（`ch1_test.js` 〜 `ch14_test.js` 相当）。これらは設計時の使い捨てで恒久保存していないが、パターンは共通なので再作成できる。検証の骨子は次の3点。

- **想定解が通ること**: 手順どおり操作して `win` になり、途中の盤面状態（座標・連結・個数）が設計意図と一致すること。
- **「折らずには解けない」証明**: 4方向×(歩く/押す)の8手を全探索するBFS（`bfs3Dpush`）で、折りを使わずには勝てないことを確認する。本編（第8〜14章）の全ステージで必須。
- **罠の確認**: 設計上の失敗ルート（押しすぎ・消滅・迂回）が意図どおり失敗すること。

盤面の組み立ては必ず `makeWorld` → `setVox` → **`assignIds` → `gravitySettle3D`** の順で行う（順序を誤ると連結グループの重力が誤判定される）。

---

## 19. 今後の検討事項（運用面）

1. **手数制限／スコアの有無**は、手数カウント＋par基準の星評価として実装済み（14章）。手数の上限制限は導入していない。
2. **本編（第8章以降）のステージ数・構成**は第8〜14章の7章・22ステージとして実装済み（全47ステージ）。手数制限やクリアランクを導入する場合の指標は今後検討する。
3. **クリアランクの基準**は「星＝手数のみ、アンドゥ/ヒント未使用＝実績バッジ」として実装済み（14章）。所要時間は採用していない。
4. **アクセシビリティ**（キー割り当てのカスタマイズ、色覚対応など）は未着手。

---

## 20. 実装メモ（参考）

- 単一HTMLファイル・依存なしの自己完結構成（BOOTH / GitHub Pages 配布）と相性が良い。ステージデータのみ外部JSON（`stages/*.json` → `build_stages.py` → `stages.js`）として分離している。
- 内部データは「3Dボクセル配列（Map）」を正とし、2Dは `{種類, 個数}` グリッドへの射影として扱う。壁なし設定は3D側 `world.open` / 2D側 `grid.open` フラグで表現し、範囲外座標も通常のMapキーとして自然に扱えるため、境界チェックの緩和だけで対応できる。
- 押し・登る・重力・危険予測はいずれも「実際の操作関数をクローンしたワールド上で試し、結果を見る」形でシミュレートしており、判定ロジックの二重実装を避けている。
- 連結グループの重力整定は、`assignIds`（識別子・グループの確定）を**必ず先に**呼んでから `gravitySettle3D` を呼ぶ必要がある（順序を誤ると、グループ未確定のまま無関係なブロックが誤って一括判定される）。
- リカバリは操作単位のスナップショットスタック（最大300件）で実装している。
- 依存なし・単一HTMLは「できれば」程度の優先度（目的はシンプルさ）。現状の小さなボクセル格子の3D描画は自前のオービットカメラ（回転行列＋投影、three.jsなし）で十分に賄えている。
- **【重要な落とし穴】DOMヘルパー `const e=id=>document.getElementById(id)` はスクリプト後半（エディタコードの直前）で定義されている。** それより前の位置でトップレベル実行されるコードが `e(...)` を呼ぶと、`const` の一時的デッドゾーン（TDZ）により `ReferenceError` が発生し、**スクリプト全体の初期化がそこで停止する**。症状は「ステージ一覧が出ない」「オプションが閉じない」「ボタンが無反応」など一見バラバラな複合不具合として現れる（実際に発生・修正済み）。定義位置より前でDOMを触る場合は `document.getElementById` を直接使うこと。関数宣言の内部で `e()` を呼ぶのは、実行が起動後になるため問題ない。
- ステージJSONの `par`（整数、省略可）はソルバーが算出した最短手数。星評価の基準に使う。`dev-tests/compute_pars.js` で一括再計算・焼き込みができる。ステージの盤面を変更したら再計算が必要。
