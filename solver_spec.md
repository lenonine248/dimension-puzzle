# ステージエディタ・ソルバー機能 設計仕様書 — draft v1

> 開発者用ステージエディタに、ステージの「解けるか」「どう解くか」「設計として健全か」を
> 自動判定するソルバーを組み込むための仕様。
> 本編（第8章以降）で繰り返し使ってきた探索検証（`bfs3Dpush` による「折らずには解けない」証明など）を、
> エディタのUIから呼べる常設機能へと一般化する。

---

## 1. 目的と位置づけ

### 1.1 二つの柱

本ソルバーは、優先度の等しい二つの目的を持つ。

1. **可否判定と最短手数（par）**: ステージが解けるか、解けるなら最短で何手か。
2. **設計不良の自動検出**: 「折らずに解けてしまう（リーク）」「折っても解けない（詰み）」「意図せぬ近道」といった、本開発で繰り返し遭遇した設計不良を機械的に見つける。

### 1.2 既存基盤との関係

ソルバーの中核は、これまでステージ検証で使ってきた幅優先探索（BFS）と同一の考え方に立つ。すなわち「実際のゲームエンジン関数をクローンしたワールド上で1手ずつ試し、到達状態を集合として管理し、勝利に達するパスを返す」方式である。判定ロジックを二重に実装せず、`Engine`／`Game`／`Game3D` の実関数（`move`・`compress`・`reconstruct`・`gravitySettle3D`・`gravityDropAvatars`・`cloneWorld` など）をそのまま呼ぶことを大原則とする。これにより、ソルバーが「解ける」と言った手順は実プレイでも必ず同じ結果になる。

### 1.3 スコープと既定値

機能を「常に働く中核」と「任意で有効化するオプション」に分ける。既定値は、日常的な設計作業で軽快に回せることを優先して決める。

- **既定で探索に含める操作**: 歩く・登る・押す・折り（＝次元切り替え）の4系統。
- **既定で有効な検出**: リーク検出（折らずに解けてしまうか）・近道検出。
- **任意オプション（既定オフ）**:
  - **数字入れ替え（swap）を探索に含める**（詳細は 6章）。
  - **詰み検出**（詳細は 4.3）。
- いずれのオプションも状態空間や計算量を大きく増やすため、必要なときだけ明示的に有効化する。既定オフのまま使っても、ソルバーの中核である可否判定・par・リーク検出は完全に機能する。

---

## 2. 探索モデル

### 2.1 状態（ノード）

1つの探索ノードは「あるワールド／グリッドの完全な盤面」に対応する。ノードは以下で構成される。

- 現在のモード（3D か 2D か）。
- そのモードの盤面（3Dなら `world.vox`、2Dなら `grid.cells`）。
- 盤面に付随する属性（`open`（壁なしフラグ）、2Dなら折り軸・front・元の3D次元）。

### 2.2 状態キー（重複除去）

探索の要は「同じ盤面を二度展開しない」ための状態キーである。本開発の探索では、アバターと押せるブロックの座標を種別付きでソート連結してキーにしてきた。ソルバーでは折りを跨ぐため、キーに**モードと盤面全体の要約**を含める必要がある。

- **3Dノード**: モード印 `3` ＋ 全ボクセルの `種別頭文字+座標`（連結グループの識別子は正規化のため含めない。座標と種別が同じなら同一盤面とみなす）をソート連結。
- **2Dノード**: モード印 `2` ＋ 折り軸・front ＋ 全セルの `種別頭文字+位置+個数` をソート連結。

> 注意: 押せるブロックの内部識別子（`id`／`grp`）は、圧縮→再構成で必ず振り直されるため、状態キーには使わない。位置と種別と個数が一致すれば同じ状態とみなす。これを誤ると「実質同じ盤面」を無限に別状態として展開してしまう。

### 2.3 手（エッジ）

1つのノードから伸びる手は、現在のモードで許された操作すべて。

- **3Dモード**: 4方向 ×（歩く・押す）＝最大8手、＋ 折り（許可された各 front ごとに1手）。
- **2Dモード**: 2方向（左右）×（歩く・押す）＝最大4手、＋ 再構成（3Dへ戻す）1手。
- **swap オプションが有効な場合のみ**、2Dモードにさらに数字入れ替えの手が加わる（6章）。既定では生成しない。

各手は「実エンジン関数を呼び、重力整定・アバター落下解決まで済ませた新しい盤面」を生成する。生成結果が勝利・敗北イベントを返したかも記録する。

### 2.4 手の適用手順（各エッジで必ず踏む順序）

本開発の検証で確立した順序を厳守する。順序を誤ると連結グループの重力が誤判定される。

- **移動（歩く／押す）**: `move`（3Dは `Game3D.move`、2Dは `Game.move`）→ `gravitySettle3D`／`gravitySettle` → `gravityDropAvatars`。
- **折り（3D→2D）**: `compress(world, axis, front)` → `gravitySettle`（2D）→ `gravityDropAvatars`（2D）。
- **再構成（2D→3D）**: `reconstruct(grid, dims)` → `gravitySettle3D` → `gravityDropAvatars`（3D）。
- クローンは毎手 `cloneWorld`（2Dグリッドも同等の複製）で行い、元ノードを破壊しない。
- 折りの front 候補は、ステージの `foldAllow` で許可されたものだけ（`rulesFromStage` と同じ規則）。全 front が不許可なら折りの手は生成されない（＝3D専用ステージ）。

### 2.5 勝敗の扱い

- 手を適用した結果、勝利イベントが出たらそのパスを解として返す（BFSなので最短）。
- 敗北（全滅・ゴール消滅）に至ったノードは、それ以上展開しない「死に節」として扱う（詰み検出に使う。4.3参照）。
- 勝ちも負けもしていないノードのみ、次の展開対象にする。

---

## 3. 判定系（解けるか・最短何手か）

### 3.1 出力

- **可否**: 解ける／解けない／探索打ち切り（未確定）の三値。「解けない」と「探索しきれなかった」を必ず区別する（重要）。
- **par（最短手数）**: 解ける場合、勝利までの手数。折り・移動・押しをそれぞれ1手として数える。
- **解パス**: 手の並び（3.3の表記）。

### 3.2 手数の数え方

- 歩く・登る・押す・折り・再構成を、それぞれ1手として等価に数える。
- 「登る」は修飾なし移動が登りにフォールバックした場合も1手（プレイヤーの入力回数と一致させる）。
- 将来 swap を含める場合、swap も1手として数える。

### 3.3 解パスの表記

テスト時に使ってきた簡潔表記を踏襲する。

- 歩く／押す: `walk(dx,dz)` / `push(dx,dz)`（2Dは `dz=0`）。人間可読な短縮形として `c(1,0)`（climb）・`p(1,0)`（push）も併記可。
- 折り: `fold(+z)` のように front を明示。
- 再構成: `unfold`。
- 例: `push(1,0) fold(+z) unfold walk(-1,0) walk(0,1)`。

### 3.4 ステップ再生

見つけた解を、エディタのテストプレイ盤面上で1手ずつ再生できる。

- 「次の手」「前の手（アンドゥ相当）」「最後まで再生」。
- 各ステップで、その手の種別（歩く／押す／折り）をラベル表示。
- 目的は「ソルバーが見つけた解が、設計者の意図した解と一致するか」を目で確かめること。意図と違う近道が見つかった場合、それ自体が設計上の気づきになる。

---

## 4. 設計不良の検出系

本ソルバーの価値の半分はここにある。以下はいずれも「探索を条件を変えて複数回走らせ、その差分から設計の狙いが成立しているかを判定する」方式で実現する。

### 4.1 リーク検出（折らずに解けてしまう）

本編ステージの根幹は「折らないと解けない」こと。これを自動で検証する。

- **折り無し探索**: 折り／再構成の手を封じ、移動・押しだけで解けるかを探索する。
- **折り有り探索**: 通常どおり全手で探索する。
- **判定**: 折り無しで解けてしまうステージは「リークあり」と警告する。本編を意図するなら設計不良。
- 本開発では毎回この2本立てを手作業で書いてきた（第8〜14章の「折る前は不可能・折った後は可能」の証明）。それを1ボタンにする。
- リークが出た場合、折り無し探索が見つけた抜け道パスを提示する（どこから素通りできるかが一目でわかる）。

### 4.2 近道検出（想定より短い解／別ルート）

- 設計者が「想定解」をエディタ上で1度プレイして記録できるようにする（記録した手数を `intended_par` とする）。
- ソルバーの par が `intended_par` より明確に短ければ「近道あり」として、その最短解を提示する。
- 想定解と同じ手数でも経路が本質的に異なる別解があれば、任意で提示（実装難度が上がるため v1 では「par の一致／不一致」までを必須とし、別解列挙は将来拡張）。

### 4.3 詰み検出（取り返しのつかない一手）【任意オプション・既定オフ】

本編は詰みを許容する方針なので、目的は「詰みの有無で合否を出す」ことではなく「どの操作が詰みを生むかを可視化」し、意図した詰みか事故かを設計者が判断できるようにすること。

**既定ではオフ**とする。理由は二つある。第一に、詰みは本編の設計方針として許容されており、常時警告する性質のものではない（毎回出ると単なるノイズになる）。第二に、後述のとおり計算量が可否判定より一段重く、日常的な「解けるか確認する」用途に付随させると探索が遅くなる。ステージが仕上がってきた段階で、意図した詰みだけが存在するかを点検するために明示的に使う。

有効化した場合の出力:

- 探索中に到達した各ノードについて「そこから勝利可能か」を判定する。
- **デッドロック・ノード**: 勝利不能になった（かつ敗北イベントも出ていない＝見た目は続行可能なのに詰んでいる）状態を列挙する。
- **危険手のハイライト**: 初期状態から1手で到達できる手のうち、その先が勝利不能になるものを列挙し「この手を打つと詰む」と表示する。
- 敗北イベント（全滅・ゴール消滅）で即座に負ける手は、詰みとは別カテゴリ（即死手）として区別表示する。

**計算量に関する注意**: 可否判定は「勝利に到達したら即終了」できるのに対し、詰み検出は「到達可能な状態空間を全て展開し、各ノードから勝利可能かを逆向きに判定する」必要がある。すなわち途中で打ち切れないため、探索は最後まで走り切る。上限（5.1）に達した場合、詰みの一覧は「網羅されていない可能性がある」と明示し、確定情報として扱わない。

### 4.4 到達可能性の要約（任意・将来拡張寄り）

- 探索で到達した状態の中で、アバターが立てた座標の集合、ゴールに個数変化が起きたか、押せるブロックが場外消滅した回数など、盤面の「使われ方」を要約する。
- 設計者が「置いたのに一度も関与しないブロック（デッドピース）」に気づける。v1 では必須にしないが、探索木から安価に出せるので候補に入れる。

---

## 5. 計算コスト管理

盤面が大きいと状態空間が指数的に膨らむ（本開発でも `overflow` に何度も当たった）。

### 5.1 上限の設定（ユーザー設定可能）

エディタのソルバーUIに、以下の上限を設定できる欄を設ける。既定値を持ちつつ、重いステージでは引き上げ／軽いステージでは絞れるようにする。

- **状態数上限**: 展開（visited へ追加）した状態の最大数（既定の目安: 20万。本開発の検証で使ってきた値）。
- **時間上限**: 探索の壁時計時間（既定の目安: 数秒〜十数秒）。UIを固めないため、探索はバックグラウンド実行（Web Worker 等）に載せることが望ましい。
- **手数上限（深さ上限）**: 任意。「par がこの手数以内にあるかだけ知りたい」用途。

### 5.2 打ち切り時の返り値

- 上限に達したら「解けない」ではなく「探索打ち切り（複雑すぎて未確定）」を返す。両者の混同は設計判断を誤らせるため、UI上もはっきり区別する（例: 「解あり／解なし（証明済み）／未確定（打ち切り）」の三表示）。
- 打ち切り時は、それまでに到達した状態数・最大到達深さを併記し、上限を上げれば確定できそうかの手掛かりを与える。

### 5.3 探索効率の工夫（実装時の指針）

- 状態キーの正規化を正しく行い（2.2）、実質同一状態の重複展開を防ぐ。これが最大の効率要因。
- BFS は最短性の保証と引き換えにメモリを食う。par が不要で可否だけ早く知りたい用途向けに、反復深化（IDDFS）など省メモリ探索を将来オプションとして検討（v1 は BFS 一本でよい）。
- 折りの手は分岐を大きく増やす。front が1方向しか許可されていないステージでは自然に分岐が減るので、`foldAllow` を尊重するだけでも効く。

---

## 6. 数字入れ替え（swap）【任意オプション・既定オフ】

探索に数字入れ替えを含めるかどうかを、ユーザーがトグルで切り替えられるようにする。**既定はオフ**。

- **なぜ既定オフか**: swap は「アバターに隣接する任意の非空セルとの個数交換」であり、2Dの1状態から生成される swap 手が多い（隣接セル数 × アバター数）。加えて交換の結果として個数が変わるため、盤面のバリエーション自体が跳ね上がり、状態空間が急激に膨らむ。設計検証の多くは swap 無しでもリークや近道を捕捉できるため、既定では軽い4系統に絞る。
- **いつオンにするか**: 数字入れ替えが解の本質になっているステージ（第3章「交換」、第10章「複製」の増築手順、第6章 6-4「合流」など）で、可否判定や par を正確に出したいとき。swap 無しの探索では、これらのステージは「解なし」と判定されてしまうため、判定結果を鵜呑みにしないよう注意が要る（下記参照）。
- **手生成**: 各2Dノードで、`swapAllow` が真なら、各アバターセルについて上下左右の非空セルとの交換を1手ずつ生成する（`Game.swap` を実関数として呼ぶ）。交換後は重力整定を通す。ステージ側で `swapAllow:false` なら、オプションが有効でも手は生成されない。
- **状態キー**: 個数がキーに含まれている（2.2）ので、swap により変化した盤面は自然に別状態として区別される。追加の対応は不要。
- **オフ時の判定表示に関する注意（重要）**: swap を含めずに探索して解が見つからなかった場合、それは「このステージは解けない」ではなく「swap 無しでは解けない」に過ぎない。UI上は、swap オプションがオフのときの「解なし」を **「解なし（swap を除く探索）」** と明示し、swap を有効にした再探索を促す。この区別を怠ると、swap が必須のステージを設計不良と誤判定してしまう。

---

## 7. UI 設計（エディタ組み込み）

開発者モードのエディタ操作バーに「ソルバー」パネルを追加する。想定する操作と表示。

- **「解を探す」ボタン**: 現在編集中の盤面に対し判定系（3章）を実行。結果（解あり／なし／未確定・par・解パス）を表示。swap オプションがオフのときの「解なし」は「解なし（swap を除く探索）」と表示する（6章）。
- **「解を再生」ボタン**: 見つけた解をテストプレイ盤面でステップ再生（3.4）。
- **「設計チェック」ボタン**: 検出系（4章）を実行。既定では「リーク: なし／あり（抜け道を表示）」「近道: なし／あり」をレポート表示。詰み検出はオプションが有効な場合のみ「詰み手: N個（一覧）」を追加表示する。
- **設定欄（オプション）**:
  - `☐ 数字入れ替え（swap）を探索に含める`（既定オフ。オンにすると探索が重くなる旨を併記）
  - `☐ 詰み検出を行う`（既定オフ。全状態を展開するため時間がかかる旨を併記）
  - 状態数上限・時間上限（5.1）
- **想定解の記録**: テストプレイで解いた手順を `intended_par` として保存するボタン（近道検出に使用）。
- 探索中はプログレス（到達状態数）を表示し、キャンセルできる。エディタUIを固めないためバックグラウンド実行を前提とする。

---

## 8. 実装しないこと（v1 の非目標）

- **自動ステージ生成**: ソルバーはあくまで「作ったステージの検査」に用いる。ランダム生成は対象外（本作は手作りが前提）。
- **難易度スコアの自動算出**: par や分岐数から難易度を数値化するのは魅力的だが、指標の妥当性検証が別途必要なため v1 では出さない（par の生値の提示にとどめる）。
- **完全な別解列挙**: par 一致／不一致の判定までを必須とし、全別解の網羅列挙は将来拡張。
- なお、数字入れ替え（swap）と詰み検出は「非目標」ではなく、**既定オフの任意オプションとして v1 に含める**（6章・4.3）。
- **配布版への露出**: ソルバーは開発者モード（`dp.dev(1)`）専用。配布版のUIには一切出さない。

---

## 9. 検証（ソルバー自体のテスト）

ソルバーが正しいことを、既存の全47ステージを使って回帰テストできる。

- **既知の可解ステージ**: 全ステージがソルバーで「解あり」になること。par が、手作業で確認済みの想定解手数と矛盾しないこと（ソルバーの par ≤ 想定解手数）。ただし swap が解に必須のステージ（第3章、6-4、第10章の増築手順など）は、swap オプションを有効にした場合にのみ「解あり」になること（オフのときは「解なし（swap を除く探索）」を返すのが正しい挙動であり、これも期待結果として明示的にテストする）。
- **リーク検出の正しさ**: 本編（第8〜14章）の全ステージが「折り無しでは解けない（リークなし）」と判定されること。逆に、意図的にリークを仕込んだテスト盤面（開発中に実際に遭遇した初版レイアウト）で「リークあり」を検出できること。
- **オプションの既定値**: swap・詰み検出がいずれも既定でオフであり、明示的に有効化したときだけ手の生成／全状態展開が行われること。
- **詰み検出の正しさ**: 意図的に詰みを仕込んだ盤面（例: 9-2 の押しすぎでゴール直下が塞がる配置）で、その手が「詰み手」として列挙されること。
- **打ち切りの正しさ**: 状態数上限を意図的に小さくすると「未確定（打ち切り）」を返し、「解なし」とは言わないこと。詰み検出が上限に達した場合は、詰み一覧を確定情報として扱わないこと。
- これらは既存の Node 上テストスイート（`ch*_test.js` 群）と同じ枠組みで自動化できる。
